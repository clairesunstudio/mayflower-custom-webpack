import React from "react";
import { Button, Icon } from "@massds/mayflower-react";
import './index.css'

const App = () => (
  <main>
    <h1>Hello world</h1>
    <Button text="Start" />
    <Icon name="alert" svgHeight={40} svgWidth={40} />
  </main>
);

export default App;
